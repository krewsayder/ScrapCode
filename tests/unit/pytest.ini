[pytest]
# Unit tests (mocked I/O) for bot.cogs / bot.services. Mirrors the acceptance
# suite's config: plain pytest + pytest-asyncio in auto mode, repo root on
# pythonpath so `import bot.*` resolves.
asyncio_mode = auto
pythonpath = ../..
testpaths = .
python_files = test_*.py
addopts = -ra